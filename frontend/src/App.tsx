import CommandCenter from "./components/CommandCenter";

/**
 * Ultron-OS — single full-screen cinematic command center (JARVIS-style). The previous tab-based
 * control panel is superseded; all interaction flows through the neural sphere + voice console.
 */
export default function App() {
  return <CommandCenter />;
}
